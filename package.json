{
  "name": "honor",
  "scripts": {
    "db": "bun run --hot src/db.ts",
    "worldGen": "bun run --hot src/generateWorld.ts",
    "server": "bun run --hot src/server.ts",
    "dev": "bun run --hot src/server.ts"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2"
  }
}